import { createTheme } from "@mui/material";

export const theme = createTheme({
    TF:{
        T: '#a0ff00',
        F: '#ff5010'
    },
    LVL:{
        ERROR: "#ff0000",
        WARNING: "#f0a100",
        LEVEL_THREE: "#0000ff",
        LEVEL_TWO: "#ff00f0",
        LEVEL_ONE: "#ffff00",
        COMPLETE: "#00ffff",
    }
});